---
title: "a920604a Labs"
description:
  background: "個人實驗性 monorepo，包含多個小型應用與共用套件，用於快速驗證想法與封裝可重複使用的 UI/認證模組。"
  challenge: "在 monorepo 中維護多個 workspace（apps / packages / workers），並提供一致的開發與部署流程。"
  solution: "採用 pnpm workspace + Vite 組合，將共用功能抽成 packages（如 auth 與 ui），並以 Cloudflare Workers 提供部分後端 API。"
  core_contributions:
    - "設計 monorepo 結構（apps/* 與 packages/*），以 workspace 套件共享 UI 與 Auth 邏輯。"
    - "實作 @a920604a/auth 與 @a920604a/ui 等套件，提供初始化 Firebase、Auth Provider 與全域 Shell 組件。"
    - "建立 workers/ebook-api 範例，示範 Cloudflare Worker + D1 的伺服器端邏輯與同步策略。"
  outcome: "形成一組可重複使用的模組與範例應用，便於快速開發新功能與部署到 Cloudflare Pages/Workers。"
tags: ["labs", "experiments"]
skills: ["react", "typescript", "chakra-ui", "firebase", "cloudflare-workers", "d1", "pnpm", "vite", "monorepo"]
github: "https://github.com/a920604a/a920604a-labs"
tag: "a920604a-labs"
pinned: false
---

a920604a Labs 是一個包含多個小型應用與共用套件的 monorepo，目的是快速驗證想法並將常用功能封裝成可重複使用的套件。

背景：專案包含 apps/*（多個小型 SPA）與 packages/*（共用套件如 auth、ui），並以 pnpm workspace 管理依賴。部分功能透過 Cloudflare Workers 與 D1 提供後端 API。

挑戰：在 monorepo 中維護一致的開發流程、跨套件型別與打包設定，並提供簡易的本地開發啟動方式。

解法與貢獻：設計 workspace 架構，將共用邏輯抽成 packages（@a920604a/auth、@a920604a/ui），在 apps/root 中集中管理路由與部署設定；建立 workers/ebook-api 範例展示 Cloudflare Worker + D1 的串接方式；提供 pnpm scripts 與 CI 指令以降低上手門檻。

成果：形成一套可複用開發模組與範例，讓新功能能在最短時間內整合到主應用並部署到 Cloudflare Pages / Workers。
